# Raycasting in SDL

## Build

Make sure you have `libsdl2` and `libsdl2-image` installed.
Build by running:

```sh
make
```

and clean up build files with:

```sh
make clean
```

## Run

Run by:

```sh
./raycaster
```
